import { MayusparesPipe } from './mayuspares-pipe';

describe('MayusparesPipe', () => {
  it('create an instance', () => {
    const pipe = new MayusparesPipe();
    expect(pipe).toBeTruthy();
  });
});
